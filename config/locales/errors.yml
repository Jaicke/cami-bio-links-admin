pt-BR:
  errors:
    messages:
      blank: não pode ficar em branco
      confirmation: não é igual a %{attribute}
      taken: já está em uso
      not_a_number: não é um número
      too_short: muito curto(a)
      invalid: inválido(a)
      required: obrigatório(a)
